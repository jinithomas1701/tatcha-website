<iscontent type="text/html" charset="UTF-8" compact="true"/>
<isif condition="${pdict.DDResponseHTML === null && pdict.redirect !== null}">
    <isredirect location="${pdict.redirect}" />
</isif>
<isprint value="${pdict.DDResponseHTML}" encoding="xmlcomment"/>
