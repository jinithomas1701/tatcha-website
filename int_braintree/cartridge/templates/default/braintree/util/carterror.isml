<isif condition="${!empty(request.custom.isBraintreeCustomError)}">
    <div class="error-form">
        <isprint value="${dw.web.Resource.msg('braintree.cart.defaulterror', 'locale', null)}"/>
    </div>
</isif>
